# Contributor Guidelines

## Commit Messages

- Use Conventional Commits for every commit message.
- Write commit messages in English.
- Use the format: `type(optional-scope): short imperative summary`.
- Examples: `feat(sensor): add settled cost total` and `fix(flow): reload after updating interval`.
